function createDefendInfo(menu) {
    let info = document.createElement("div");
    info.className = "infoParams";
    info.id = "infoParamsDefend";
    info.style.display = "none";
    menu.appendChild(info);
}