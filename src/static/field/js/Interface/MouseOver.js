function moveTip(e) {
    /*var unitTip = document.getElementById("unitTip").style;
    var matherShipTip = document.getElementById("matherShipTip").style;

    var w = 0; // Ширина слоя
    var x = e.pageX; // Координата X курсора
    var y = e.pageY; // Координата Y курсора

    if ((x + w + 10) < document.body.clientWidth) {
        // Показывать слой справа от курсора
        unitTip.left = x + 'px';
        matherShipTip.left = x + 'px';
    } else {
        // Показывать слой слева от курсора
        unitTip.left = x - w + 'px';
        matherShipTip.left = x - w + 'px';
    }
    // Положение от верхнего края окна браузера
    unitTip.top = y + 20 + 'px';
    matherShipTip.top = y + 20 + 'px';*/
}
